module.exports = {
    languageOptions: {
        globals: {
          fetch: false, // Modify as needed for your globals
          WebSocket: true
        },
        parserOptions: {
            sourceType: module
        }
    }
}